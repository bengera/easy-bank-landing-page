const hamMenu = document.querySelector('.hamburger-menu');
// const popOutBox = document.querySelector('.nav__popout-box');


hamMenu.addEventListener('click', () => {
    hamMenu.classList.toggle('active');
    // popOutBox.classList.toggle('show');


});

